# layers/screener.py — no Streamlit imports
import pandas as pd


def run_screener(universe: str, filters: dict) -> pd.DataFrame:
    """Return a ranked DataFrame of tickers matching the given filters.

    Implemented in Phase 2.
    """
    raise NotImplementedError("Screener layer is implemented in Phase 2")
